/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    formats: ["image/avif", "image/webp"],
    domains: ["tutorial.saleor.cloud"],
  },
};

module.exports = nextConfig;
